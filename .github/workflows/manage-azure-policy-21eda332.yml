name: manage-azure-policy-21eda332
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'CMMC_L3/policies/_Preview_:_Audit_Log_Analytics_Agent_Deployment_-_VM_Image_(OS)_unlisted_32133ab0-ee4b-4b44-98d6-042180979d50/**'
#      - .github/workflows/manage-azure-policy-21eda332.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_21eda332}}
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          CMMC_L3/policies/_Preview_:_Audit_Log_Analytics_Agent_Deployment_-_VM_Image_(OS)_unlisted_32133ab0-ee4b-4b44-98d6-042180979d50/**
